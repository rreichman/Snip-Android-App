package snip.androidapp;

import android.content.Context;
import android.content.Intent;
import android.content.pm.PackageManager;
import android.os.AsyncTask;
import android.os.Parcelable;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import java.io.File;

/**
 * Created by ranreichman on 8/17/16.
 */
public class DataCacheManagementTests
{
    @Test
    public void testDeleteAllInformationFiles()
    {
        // Make sure the files exist
        // Delete them
        // Make sure they don't exist
        Assert.fail();
    }

    @Test
    public void testSaveObjectToFile()
    {
        // Save an object to file and then read it back
        Assert.fail();
    }

    @Test
    public void testDeleteActivityInformationFiles()
    {
        // Make sure the files exist
        // Delete them
        // Make sure they don't exist
        Assert.fail();
    }

    @Test
    public void testRetrieveSnipDataFromBundle()
    {
        // Create a bundle with snip data, including query
        // retrieve the data from the bundle
        // make sure everything is ok
        Assert.fail();
    }

    @Test
    public void testRetrieveSnipDataFromFile()
    {
        // Save snip data into a file
        // retrieve the data from the file
        // make sure everything is ok
        Assert.fail();
    }

    @Test
    public void testRetrieveQueryDataFromFile()
    {
        // Save query into a file
        // retrieve the query from the file
        // make sure everything is ok
        Assert.fail();
    }

    @Test
    public void testFullAppCaching()
    {
        // Create data
        // Save data and query into a file
        // retrieve the data and the query from the file
        // delete the data using the relevant function
        // Make sure everything is ok
        Assert.fail();
    }
}
